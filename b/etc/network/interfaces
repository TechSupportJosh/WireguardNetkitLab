# Initialise loopback interface
auto lo
iface lo inet loopback

# Set up eth0
iface eth0 inet static
    address 12.0.0.2
	gateway 12.0.0.1
	hwaddress 00:00:00:00:00:BB
	
# Set up eth0
iface eth1 inet static
    address 16.0.0.1
	hwaddress 00:00:00:00:00:CC
